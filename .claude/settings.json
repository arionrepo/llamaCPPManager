{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "claude-3-5-haiku-20241022",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q 'src/llamacpp_manager/' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"REMINDER: Python CLI source was edited. The CLI is installed via pipx as a non-editable copy — changes are NOT live until you run: pipx reinstall llamacpp-manager\"}}' || true"
          }
        ]
      }
    ]
  }
}
